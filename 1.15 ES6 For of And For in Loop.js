// For in loop
var all = ['one', 'two', 'three', 'four'];

// For in give return property
for(let props in all){
    console.log(all[props]);
}

// Line break
console.log('\n');

// For of give return value
for(let props of all){
    console.log(props);
}